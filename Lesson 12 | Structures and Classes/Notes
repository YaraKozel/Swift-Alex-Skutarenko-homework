class Student {
var name = "No name"
var age = 20
}

let student = Student() — екземпляр класу

student.name = "Alex" - можна міняти властивості класу

class Student {
var name : String
var age : INT
} - конструктор класу

найпростіший конструктор — функція init()
inut() {
...
}
init(_name: String, _age: Int) {
name = _name
age = _age
}

self - вказує на даний екземпляр
self.name
salf.age

Структура створює конструктор автоматично

Щоб змінити клас, треба:
property var
екземпляр let/var

Щоб змінити структуру:
property var
structure var

Struct is value type, class - reference type.

Можна створювати func з однаковими назвами, параметрами, але різними типами, які вони приймають.

struct - невелика кількість даних
class - для маштабних робіт

typer - UpperCamelCase
properties - lowerCamelCase

=== means isentical to some instance of class
!== not identical to

if oneClass === anotherClass {
print("Yes")
}

