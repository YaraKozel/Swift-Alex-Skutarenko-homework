% - залишок від ділення
10 % 3 = 1

с = ( a < b ) ? a : b
означає:

if a < b {
c = a
} else {
c = b
}

range 0...5 включає 5
range 0..<5 лише до 4

Оператор, що перевіряє nil
a ?? b - це те саме, що :
a != nil ? a! : b

