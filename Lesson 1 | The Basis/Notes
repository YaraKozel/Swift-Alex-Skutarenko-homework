// Swift Basis

let — константа. Тип даних всередині не змінюється.
var — перемінна. Можна привласнювати нові значення цього ж типу.

if var = (String)() {
var = 5 
EROR!
}

\() - інтерполяція. Дозволяє виводити всередину String константи та перемінні.
// — коментуємо рядок. 
/* bjkdnfgfj */ — коментуємо блок коду всередині.
\n — друк з нового рядка.
